Structure set
=============

Add a JSON here that lists your structure and when/where it can spawn
(e.g. spacing, separation, salt). The structure set is referenced by the
dimension's chunk generator so the game knows to place your house in the world.

Example: mob_wrangler_house.json
  References the structure from ../structure/ and sets placement rules.
